# 2026-03-11

- Session started.
- Discussed migrating OpenClaw + workspace from current VPS to a new VPS while AyurNod remains live.
- Agreed safest path: fresh OpenClaw install on new VPS, then migrate full state directory (`~/.openclaw`) + workspace, ideally under separate Linux user (`openclaw`) to avoid conflicts.
- Prepared command-based migration checklist for old VPS and new VPS.
- User requested committing all workspace changes before doing separate installation on the new VPS.
- Verified on new VPS that live AyurNod repo is present at `/var/www/AyurNod` (git remote configured to `https://github.com/pankaj-mahaur/AyurNod.git`, branch `main`).
- Cloned a fresh dev copy to `/root/AyurNod` from `https://github.com/pankaj-mahaur/AyurNod.git`.
- User requested: update memory properly and push workspace.
- Updated `memory/2026-03-11.md` and `MEMORY.md` with new VPS AyurNod paths.
- Committed workspace changes locally (`5ec54f6`) but push is currently blocked on this VPS due to missing GitHub SSH credentials (no key configured in `/root/.ssh`).
- Generated a new SSH key at `/root/.ssh/id_openclaw_workspace` and configured `/root/.ssh/config` for `github.com`; push remains blocked until the new public key is added to GitHub/deploy keys.
- GitHub deploy key was added; workspace branch `master` successfully pushed to `origin`.
- Generated dedicated AyurNod SSH key (`/root/.ssh/id_ayurnod`) and switched `/root/AyurNod` remote to `git@github-ayurnod:pankaj-mahaur/AyurNod.git` (awaiting deploy-key addition for auth).
- On user request, switched workspace flow to `main`: force-updated `origin/main` from local branch, then renamed local `master` -> `main` and set upstream to `origin/main`.
