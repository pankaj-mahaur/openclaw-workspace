# 2026-03-11

- Session started.
- Discussed migrating OpenClaw + workspace from current VPS to a new VPS while AyurNod remains live.
- Agreed safest path: fresh OpenClaw install on new VPS, then migrate full state directory (`~/.openclaw`) + workspace, ideally under separate Linux user (`openclaw`) to avoid conflicts.
- Prepared command-based migration checklist for old VPS and new VPS.
- User requested committing all workspace changes before doing separate installation on the new VPS.
- Verified on new VPS that live AyurNod repo is present at `/var/www/AyurNod` (git remote configured to `https://github.com/pankaj-mahaur/AyurNod.git`, branch `main`).
- Cloned a fresh dev copy to `/root/AyurNod` from `https://github.com/pankaj-mahaur/AyurNod.git`.
- User requested: update memory properly and push workspace.
- Updated `memory/2026-03-11.md` and `MEMORY.md` with new VPS AyurNod paths.
- Committed workspace changes locally (`5ec54f6`) but push is currently blocked on this VPS due to missing GitHub SSH credentials (no key configured in `/root/.ssh`).
- Generated a new SSH key at `/root/.ssh/id_openclaw_workspace` and configured `/root/.ssh/config` for `github.com`; push remains blocked until the new public key is added to GitHub/deploy keys.
- GitHub deploy key was added; workspace branch `master` successfully pushed to `origin`.
- Generated dedicated AyurNod SSH key (`/root/.ssh/id_ayurnod`) and switched `/root/AyurNod` remote to `git@github-ayurnod:pankaj-mahaur/AyurNod.git` (awaiting deploy-key addition for auth).
- On user request, switched workspace flow to `main`: force-updated `origin/main` from local branch, then renamed local `master` -> `main` and set upstream to `origin/main`.
- User assigned a major mobile-only parity task: make AyurNod mobile match website functionality/design and be production-ready.
- Created and pushed AyurNod branch `phanx-mobile-design-functionality-and-updations` from latest `main`.
- User explicitly requested **not to touch live deploy** and to run/testing via mobile Expo server from VPS.
- Expo tunnel was started on VPS for testing (`exp://xkfghmg-anonymous-8081.exp.direct`).
- Completed and pushed multiple checkpoint commits on the mobile parity branch:
  - `3406f4b` — Dosha/Prakriti flow aligned to website 12-question parity.
  - `e5a8814` — major chat parity work (attachments + in-chat plan flow + profile parity updates).
  - `cd056ee` — mobile API default/fallback fixes + guest chat mode.
  - `abd5559` — login/auth handling fix to avoid misleading "session expired" on login.
  - `287dc2b` — better auth rate-limit UX handling.
  - `ebbee46` — disabled password auto-capitalize/autocorrect on mobile auth fields.
- Key technical discovery: `https://ayurnod.in/api/auth/*` and `https://staging.ayurnod.in/api/auth/*` return 404 (website Next app routes), while auth API is live at `https://api.ayurnod.in`.
- Backend auth route has strict limiter (`5` attempts / `15 minutes`), which caused user-facing "Too many requests" during repeated login attempts.
- User requirement clarified: chat should work in guest mode without login, but synced history/profile/plan should require login, matching website behavior expectations.
- Current unresolved issue at last exchange: user still reported "invalid email or password" despite existing website account; next step offered was immediate backend password reset for the account.
