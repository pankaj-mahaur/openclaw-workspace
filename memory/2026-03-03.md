# 2026-03-03

- Session started.
- OpenFlow tester baseline run: reviewed master config + controller/tester scripts, controller/gateway status PASS, tester for openai-codex/gpt-5.3-codex gmail_1 blocked as expected by missing `OPENAI_API_KEY_GMAIL1`; recorded failure via controller script and updated `agents/tester/reports/test-results-latest.md`.
- Initialized OpenFlow multi-provider control-plane scaffold:
  - Added master config/state/catalog under `config/openflow/`.
  - Added scripts `scripts/openflow/controller.js` (routing + gateway ops) and `scripts/openflow/tester.js` (API health checks).
  - Created dedicated sub-agent workspaces for Searcher and Tester under `agents/` with AGENTS/SOUL/USER/HEARTBEAT/MEMORY + daily memory + report stubs.
  - Spawned first Searcher and Tester sub-agent runs on model `openai-codex/gpt-5.3-codex`.
- User clarified this work must remain strictly in OpenClaw workspace (not AyurNod) — acknowledged.
- Added Cerebras provider scaffold to OpenFlow master config (openai-compatible base URL + account env slots + model entries).
- Ran live tester check for Cerebras `gmail_1` credential; result: HTTP 401 `Wrong API Key` (invalid/mismatched key), recorded in tester report.
- Pulled latest Cerebras official docs for supported models + free-tier limits; updated `config/openflow/free-model-catalog.json`, `agents/searcher/reports/free-models-latest.md`, and refined Cerebras limits in `config/openflow/master-config.json`.
- On user request, retested newly shared Cerebras key against tester + direct `/v1/models`; both returned HTTP 401 `Wrong API Key`.
- Verified via official Cerebras sample endpoint (`/v1/chat/completions`) and auth modes: Bearer also returns 401, `x-api-key` returns 403, confirming current issue is credential-side (not local OpenFlow script config).
- Received Groq key, added Groq provider config in OpenFlow, validated `gmail_1` with tester (PASS 200), verified live completions for `llama-3.1-8b-instant` and `meta-llama/llama-4-scout-17b-16e-instruct`, and switched active route to Groq model via controller select.
- On user request, generated full Groq free-model details and enforced quality-only routing shortlist in OpenFlow (removed weak/safety/audio/specialized models from default routing). Updated controller scoring/filters and reselected active model to `meta-llama/llama-4-scout-17b-16e-instruct`.
- Added Groq keys for `gmail_2` and `gmail_3`; both passed tester + direct chat completion smoke tests on `meta-llama/llama-4-scout-17b-16e-instruct`.
- User requested strict separation: main/default OpenClaw config must stay independent; free-tier multi-key routing is subagent-only. Implemented split config/state (`config/openflow/subagent/*`) and wrapper scripts (`subagent-controller.sh`, `subagent-tester.sh`), while resetting main config/state back to default-only route.
- In subagent-isolated Groq setup, ingested updated keys for gmail_1 and new keys for gmail_4/5/6; test matrix now PASS for gmail_1,3,4,5,6 and FAIL for gmail_2 (`invalid_api_key`).
- Retested with new gmail_2 Groq key; now PASS. Current subagent Groq account health: gmail_1..gmail_6 all passing on Scout model.
- Added subagent-only live quota visibility + switching controls: `subagent-rate-probe.sh` (captures rate-limit headers), `subagent-controller.sh quota`, `maybe-switch`, and `select-next` for rotation/failover before exhaustion.
- Added `subagent-autoswitch.sh` one-shot command (probe active route + conditional pre-limit switch in same run).
- Installed subagent autoswitch cron (every minute), added subagent keys env for cron, and implemented task-safety guard flow (`lock`/`unlock`) so quota-based switching is deferred while a long task is in progress.
- Implemented full subagent task checkpoint/resume framework with task tokens and persisted snapshots (`subagent/tasks/*.json`), plus helper scripts for start/save/resume/end. Verified deferred-switch behavior during lock and clean resume/complete flow.
- Added storage/token hygiene controls for autosave: checkpoint debounce, summary length cap, tiny-progress suppression, per-task checkpoint cap, sparse cron logging, and daily maintenance prune/compaction job.
- Created consolidated runbook doc: `notes/OPENFLOW-SUBAGENT-OPERATIONS.md`.
- Implemented milestone-based autosave policy defaults for checkpoints (25 min / 12% progress / milestone bypass), plus checkpoint caps and pruning to control storage/context growth.
- Switched subagent Groq routing to quality-first best-of-best mode: Maverick as primary, Scout as throughput fallback; updated subagent catalog scoring so selection reflects this priority.
- Implemented strict per-request RPM/TPM gate for subagent API calls (`subagent-router.sh` acquire-route/settle/status) and added gated request wrapper (`subagent-safe-chat.sh`) to prevent over-limit API usage.
- Enabled wrapper-only strict mode for subagent setup and installed periodic process audit (`subagent-strict-audit.sh`) to kill/log ungated direct provider API CLI calls.
- Ran fresh smoke tests for both subagent roles: Tester passed status/rate-probe/router-acquire on Groq Maverick; Searcher validated quality-order alignment and updated report section.
- Added daily Searcher automation (`searcher-daily-sync.sh/js`) to detect new/removed free Groq models and limits changes once/day, update catalog/report/daily diff JSON, and log memory.
- Built and started live GUI dashboard service (`apps/openflow-dashboard`) with status/health/candidates/daily-diff/rate-gate views at `http://127.0.0.1:18888`.
- Scheduled searcher daily sync cron and dashboard auto-start on reboot.
- Switched dashboard to token-protected public mode on `0.0.0.0:18888` with secure token in `config/openflow/subagent/dashboard.env`; verified public-IP health endpoint access with token.
- Added Searcher anti-malicious safeguards: strict source policy allowlist + blocked internal endpoints + forbidden-keyword checker, with daily cron enforcement and policy logs.
- User requested a single short-name for the full subagent automation stack; set codename: **OF-Guard**.
- User requested 3-role work-division pipeline with role-specific responsibilities:
  - Search Engine (trusted-link discovery)
  - Web Crawler (structured extraction from trusted links)
  - Summarizer (question-focused final brief)
- Added role pipeline spec at `config/openflow/subagent/role-pipeline.json` with per-role model preference order and output contracts.
- Attempted persistent subagent sessions; platform requires thread hooks unavailable in current webchat context, so switched to run-based subagent deployment pattern.
- Launched run-based role readiness checks (`ofg-search-engine`, `ofg-web-crawler`, `ofg-summarizer`) via `sessions_spawn` mode=run.
- Persisted default workflow preference in long-term and user context:
  - Updated `MEMORY.md` with OF-Guard default pipeline preference (Search Engine -> Web Crawler -> Summarizer).
  - Updated `USER.md` notes with same default divided workflow preference.
- Updated operations markdown with default role pipeline section in `notes/OPENFLOW-SUBAGENT-OPERATIONS.md`.
- Upgraded OF-Guard dashboard UI/UX with richer controls and telemetry:
  - Added server endpoints: `/api/summary`, `/api/events`, `/api/pipeline`, `/api/policy`.
  - Added KPI cards, refresh controls, model/provider filters, health filters, gate utilization bars, daily diff breakdown, role pipeline visualization, policy/event panels, and raw JSON inspectors.
  - Restarted dashboard service and validated live summary endpoint response.
- Per user check request, re-verified dashboard runtime health:
  - Service running (`dashboard-status` OK)
  - API endpoints all 200 with token (`health/summary/state/config/daily/gate/pipeline/policy/events`)
  - Token guard behavior correct (`/api/summary` = 401 without token, 200 with token)
  - Static assets load OK (`/`, `/app.js`, `/styles.css` all 200)
  - JS syntax checks pass for `server.js` and `public/app.js`.
- Updated scheduling per user preference: Searcher + Tester jobs now run **2x/day** (UTC). Added `scripts/openflow/subagent-tester-scheduled.sh` and cron entries at `03:40,15:40` (searcher) and `03:50,15:50` (tester active-route check).
- Implemented multi-provider automation upgrade:
  - `searcher-daily-sync.js` now syncs **Groq + Cerebras** model availability (OpenAI-compatible `/models`) and keeps `daily-sync-latest.json` in multi-provider format.
  - Added Cerebras model set in subagent config: `gpt-oss-120b`, `llama3.1-8b`, `qwen-3-235b-a22b-instruct-2507` (preview), `zai-glm-4.7` (preview).
  - Added preview lifecycle logic: preview entries are auto-removed from config when provider stops listing them (or tester matrix confirms model-not-found style failures across checks).
  - Upgraded tester to support real model validation via `--probe chat` (not just `/models` listing).
  - Added `subagent-tester-matrix.sh` for scheduled all-model/account validation and preview cleanup; wired through `subagent-tester-scheduled.sh`.
  - Ran live smoke: searcher multi-provider sync PASS; scheduled tester run PASS on all currently keyed combos.
- Upgraded OF-Guard dashboard to modern low-load live mode:
  - New compact APIs: `/api/pack/live` and `/api/pack/full` to reduce request fan-out.
  - Frontend now defaults to **10s refresh**, uses lightweight live payload each tick and full payload every ~60s.
  - Added live status strip (pulse dot + mode badge + countdown) and background-tab slow mode (>=30s) to reduce VPS load.
  - Restarted dashboard and validated token auth + live/full pack endpoints.
- Added new dashboard section: **Live Subagent Model Usage**.
  - Shows router live model/account, tester cadence + latest check, searcher cadence + latest sync, and role-level routing map for Search Engine/Web Crawler/Summarizer.
  - UI refreshed to be more colorful/interactive/responsive (gradient cards, worker cards, hover polish) while preserving low-load fetch behavior.
- Explicit Cerebras test status check performed for all configured Cerebras models; all currently return missing-key error because `CEREBRAS_API_KEY_GMAIL1` or `CEREBRAS_API_KEY` is not set in runtime env.
- Pending next action: once Cerebras key is provided in runtime env, rerun full tester matrix for all 4 Cerebras models and reflect pass/fail state in dashboard health cards.
- UX direction confirmed by user: keep OF-Guard dashboard modern, colorful, responsive, and interactive while maintaining low VPS load with smart refresh strategy.
- Dashboard refinement shipped: Account Health Feed now compact by default (row-limit selector + fail-first toggle + provider summary chips + fixed-height scroll table), and overall UI moved toward dark premium black-glass morphism styling.
- Added dedicated role-context workspaces for OF-Guard pipeline roles:
  - `agents/search-engine/*`
  - `agents/web-crawler/*`
  - `agents/summarizer/*`
  Each now has AGENTS/SOUL/HEARTBEAT/MEMORY/USER files tailored to role behavior.
- Updated `config/openflow/subagent/role-pipeline.json` to include `contextFiles` per role (version bump to 1.1.0) so role prompts can consistently reference these docs.
- Updated operations doc with explicit role-context file mapping for maintenance clarity.
- Fixed dashboard `Policy + Live Events` overflow: responsive auto-fit grid + `pre` wrapping (`white-space: pre-wrap`, `overflow-wrap:anywhere`, `word-break`) so long log lines no longer break layout.
- On user request, updated long-term + ops docs:
  - `MEMORY.md`: added durable OF-Guard dashboard UX preference (dark premium, compact sections, low-load behavior).
  - `notes/OPENFLOW-SUBAGENT-OPERATIONS.md`: expanded dashboard section with live-pack refresh strategy, compact health feed controls, overflow fix notes, and theme direction.
- User preference added: whenever a response/result is based on delegated subagent work, include a small marker in reply text: `*help by sub agents*`.
- User provided Cerebras gmail_1 key for setup; saved to `config/openflow/subagent/keys.env` as `CEREBRAS_API_KEY_GMAIL1` and fallback `CEREBRAS_API_KEY` (value not logged in memory).
- Immediate Cerebras validation run (`gpt-oss-120b`, `llama3.1-8b`, `qwen-3-235b-a22b-instruct-2507`, `zai-glm-4.7`) failed with 401 `wrong_api_key`.
- Fixed env propagation bug in scripts by exporting sourced vars (`set -a`):
  - `scripts/openflow/searcher-daily-sync.sh`
  - `scripts/openflow/subagent-tester-scheduled.sh`
  - `scripts/openflow/subagent-tester-matrix.sh`
- After export fix, searcher now correctly detects Cerebras auth failure as sync error (instead of false "no key").
- Investigated user concern "OpenClaw may not support Cerebras key": confirmed not an OpenClaw compatibility issue.
  - Direct raw curl to `https://api.cerebras.ai/v1/models` with Bearer auth returns `401 wrong_api_key`.
  - `x-api-key` header was also tested and returned `403 Not authenticated` (docs expect Bearer).
  - Live docs still specify OpenAI-compatible usage with `baseURL=https://api.cerebras.ai/v1` and Bearer key.
- User provided second Cerebras key for gmail_1; replaced in `keys.env` and reran direct+tester validations.
- Result unchanged: `GET /v1/models` and all 4 model chat probes return `401 wrong_api_key`.
- User requested complete Cerebras removal from OF-Guard; applied cleanup:
  - Removed Cerebras provider from `config/openflow/subagent/master-config.json`.
  - Removed Cerebras items from `config/openflow/subagent/free-model-catalog.json`.
  - Purged Cerebras health/tests entries from `config/openflow/subagent/runtime-state.json`.
  - Removed `CEREBRAS_API_KEY*` entries from `config/openflow/subagent/keys.env`.
  - Refactored `scripts/openflow/searcher-daily-sync.js` to Groq-only sync and regenerated daily sync state.
  - Updated operations notes to Groq-only provider setup.
- User requested durable safeguard: even if asked accidentally, do not re-lock coding access (avoid switching tools profile back to messaging/locked mode without explicit reconfirmation).
- Added Gemini free-tier provider to OF-Guard subagent stack:
  - Updated `config/openflow/subagent/keys.env` with `GEMINI_API_KEY_GMAIL1..5` (+ fallback `GEMINI_API_KEY`).
  - Added Gemini provider in `config/openflow/subagent/master-config.json` with best-model shortlist (`models/gemini-2.5-pro`, `models/gemini-2.5-flash`) and 5 account slots.
  - Added Gemini catalog entries in `config/openflow/subagent/free-model-catalog.json` and updated operations note to Groq+Gemini.
  - Hardened `scripts/openflow/rate-probe.js` to support both `max_completion_tokens` and `max_tokens` payload styles (OpenAI-compatible variance).
  - Patched `scripts/openflow/searcher-daily-sync.js` to preserve non-Groq catalog items during Groq sync (prevents Gemini manual entries from being wiped).
- Gemini validation + RPM/TPM probe run (all keys x shortlist):
  - `models/gemini-2.5-pro`: 0/5 pass, all 429 with free-tier quota-limit-0 errors (temporarily disabled in routing config).
  - `models/gemini-2.5-flash`: 4/5 pass, `gmail_4` currently 429.
  - Gemini responses do not expose `x-ratelimit-*` headers via OpenAI-compatible endpoint, so dynamic RPM/TPM header capture remains empty `{}` for successful probes.
- On user request, performed live web research for Gemini RPM/TPM/RPD because headers were missing:
  - Official Gemini docs (latest) now route exact per-model limits to AI Studio rate-limit page; docs no longer publish full non-batch model tables publicly.
  - Pulled Google AI Developers Forum references; staff reply includes Gemini 2.5 Flash limits (Free tier 10 RPM / 250k TPM / 250 RPD).
  - Kept Gemini Pro disabled due observed quota-limit-0 on all keys, but set reference baseline in config/catalog (5 RPM / 250k TPM / 100 RPD) for future re-enable checks.
  - Updated `config/openflow/subagent/master-config.json`, `config/openflow/subagent/free-model-catalog.json`, and operations note with live-search validated baseline limits + caveat to re-check AI Studio per project/tier.
- User re-sent Gemini gmail_4 key; verified it matches stored value in `keys.env` and re-tested:
  - `models/gemini-2.5-flash` on gmail_4: 429 RESOURCE_EXHAUSTED
  - `models/gemini-2.5-pro` on gmail_4: 429 RESOURCE_EXHAUSTED
  - rate-probe for flash on gmail_4 also 429; key appears valid format but project/account quota currently exhausted.
- On user request ("non-working keys ko kar do"), applied hard-disable for failing Gemini account:
  - Removed Gemini `gmail_4` account from `config/openflow/subagent/master-config.json` (active Gemini accounts now: gmail_1, gmail_2, gmail_3, gmail_5).
  - Set `GEMINI_API_KEY_GMAIL4=''` in `config/openflow/subagent/keys.env` with disable comment.
  - Cleaned stale Gemini gmail_4 health/tests entries from `config/openflow/subagent/runtime-state.json`.
  - Updated operations note to reflect gmail_4 hard-disabled for repeated quota failures.
- User provided NVIDIA Build API key and requested: research first, then add all relevant free NVIDIA models to OF-Guard.
- Live NVIDIA validation performed:
  - `GET https://integrate.api.nvidia.com/v1/models` with provided key returned 200 and ~185 model IDs.
  - `POST /v1/chat/completions` smoke test on `meta/llama-4-maverick-17b-128e-instruct` returned 200 (`OK`).
- Research in progress: extracting reliable "free/prototyping-eligible" subset from NVIDIA catalog/docs before integrating into OF-Guard provider config.
- User requested: keep best models prioritized, add remaining models too, avoid VPS load increase, and paginate "Models (Quality + Limits)" dashboard section.
- Implemented NVIDIA model strategy update:
  - NVIDIA provider now has broad enabled inventory (153 enabled) with best models kept at top via priority ordering.
  - Kept 4 known-problem models disabled (`meta/llama-4-scout-17b-16e-instruct`, `deepseek-ai/deepseek-v3.2`, `moonshotai/kimi-k2.5`, `z-ai/glm5`) due repeated timeout/404 behavior on current account.
- Implemented low-load safeguard for scheduled tester matrix:
  - Added provider caps in `scripts/openflow/subagent-tester-matrix.sh`.
  - New env knobs: `OPENFLOW_TESTER_MATRIX_MAX_PER_PROVIDER` (default 60) and `OPENFLOW_TESTER_MATRIX_MAX_NVIDIA` (default 20).
  - Active route model is always included even if outside cap.
- Implemented dashboard pagination for Models table:
  - Added rows-per-page selector, prev/next controls, page indicator, and visible range meta.
  - Updated files: `apps/openflow-dashboard/public/index.html`, `apps/openflow-dashboard/public/app.js`, `apps/openflow-dashboard/public/styles.css`.
- Updated ops doc `notes/OPENFLOW-SUBAGENT-OPERATIONS.md` to reflect NVIDIA broad inventory mode and tester matrix load caps.
- User reported OF-Guard slowdown because subagents were repeatedly hitting same model/account slot; requested best-model-first but distribute across different mail keys.
- Implemented router-level account spreading in `scripts/openflow/router.js`:
  - Removed sticky active-route score boost.
  - Added allocator-aware candidate ranking (`routeAllocator`) with utilization/hot-route penalties.
  - Added same-model account rotation bias to avoid repeatedly selecting same mail for consecutive calls.
  - Added optional `--worker` affinity support for per-worker stable-but-non-colliding routing.
- Updated `scripts/openflow/subagent-safe-chat.js` to accept `--worker` and pass it to router acquire-route.
- Verified with live acquire-route sequence: router now rotates across Groq accounts (gmail_1..gmail_5 observed in sequence) instead of sticking to one account.
- Reset gate after tests and reselected active route; current selected active route became `nvidia / qwen/qwen3.5-397b-a17b / gmail_1` by quality score.
- User asked to fully remove failing models (not just disable) while keeping best routing quality.
- Applied fail-model hard removal in OF-Guard subagent config:
  - Removed from `master-config.json`: `models/gemini-2.5-pro`, `meta/llama-4-scout-17b-16e-instruct` (NVIDIA namespace), `deepseek-ai/deepseek-v3.2`, `moonshotai/kimi-k2.5`, `z-ai/glm5`.
  - Removed same entries from `free-model-catalog.json`, fallback order references, and runtime `health/tests` stale records.
  - Net cleanup: config models removed=5, catalog entries removed=5, health rows removed=7, test rows removed=12.
- Post-clean state:
  - Gemini now active only on `models/gemini-2.5-flash`.
  - NVIDIA now 153 models (all enabled) after failed IDs were removed.
- Updated `notes/OPENFLOW-SUBAGENT-OPERATIONS.md` to reflect fail-model removal status.
- User flagged dashboard gap: "Live Subagent Model Usage" looked stale.
- Fixed dashboard backend + UI for per-role live routing visibility:
  - `apps/openflow-dashboard/server.js` now reads `state.routeAllocator.workerAffinity` and reports role-specific provider/model/account instead of copying one global active route for all roles.
  - `apps/openflow-dashboard/public/app.js` role cards now display `routeKey` (or fallback indicator).
  - Restarted dashboard service.
- Seeded worker-affinity routes via router `--worker` calls:
  - search-engine -> groq:maverick:gmail_1
  - web-crawler -> groq:maverick:gmail_2
  - summarizer -> groq:maverick:gmail_3
- Verified `/api/summary` now returns distinct roleRouting entries per role/account.
- On user request due rising VPS load, turned OFF OF-Guard background load:
  - Stopped dashboard via `scripts/openflow/dashboard-stop.sh`.
  - Disabled all OpenFlow cron jobs by filtering `/root/.openclaw/workspace/scripts/openflow/` entries out of crontab.
  - Saved cron backup at `config/openflow/subagent/cron-backup-20260303T145348Z.txt` for quick restore.
- User requested deploying a subordinate orchestrator agent named "Paplupal" (same style/memory intent, NVIDIA-best focus, can coordinate role subagents to reduce token usage).
- Platform constraint confirmed in this webchat context: persistent thread-bound subagent sessions are unavailable (`thread=true` hooks missing), so continuous always-on child session cannot be kept.
- Implemented workaround: spawned run-mode subagent profile for Paplupal and captured reusable orchestration template.
- Paplupal template now available for use on-demand: brief -> pipeline split -> NVIDIA-heavy routing guidance -> checkpointed compact return.
- User clarified permanent operating preference:
  - Priority is best possible final output quality.
  - Paplu should keep direct user-facing token usage minimal/compact.
  - Subagents may use higher token budgets for deeper research/execution, then return condensed outputs.
  - Paplu should do extra hard-thinking itself only when subagent output quality is insufficient.
- User requested clear workflow chart/explanation of this mode; will use Paplu + Paplupal orchestration pattern going forward.
- User provided NVIDIA key for gmail_2; added `NVIDIA_API_KEY_GMAIL2` to `config/openflow/subagent/keys.env`.
- Updated `config/openflow/subagent/master-config.json` NVIDIA account `gmail_2` status to active.
- Validation:
  - `GET https://integrate.api.nvidia.com/v1/models` with gmail_2 key returned 200 (185 models).
  - `subagent-rate-probe` on nvidia maverick with gmail_2 returned PASS 200.
  - `subagent-tester` chat probe on nvidia maverick with gmail_2 returned PASS 200.
