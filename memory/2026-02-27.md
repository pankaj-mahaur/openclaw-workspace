# Daily Notes — 2026-02-27

## Session Summary
- Synced workspace from GitHub repo `pankaj-mahaur/openclaw-workspace`.
- Identified active AyurNod work branch: `website-optimization-and-updation` in `/root/AyurNod/AyurNod`.
- Clarified path mismatch (`/root/Ayurnod` vs `/root/AyurNod`).
- Reverted accidental Telegram token changes made inside AyurNod project.
- Configured Telegram channel in OpenClaw itself (bot `@Paplupalbot`) and verified channel status as running.
- Updated memory policy to enforce daily files + separate high-value `MEMORY.md`.

## Work Log
- Workspace pull + branch tracking fixed.
- AyurNod repo state confirmed clean on `website-optimization-and-updation` after reset.
- OpenClaw `channels add --channel telegram --token ...` completed successfully.
- Backfilled missing daily memory files for 2026-02-20, 21, 22, 23, 25, 26.

## Decisions / Follow-ups
- Keep **daily activity** in `memory/YYYY-MM-DD.md`.
- Keep `MEMORY.md` only for durable long-term context and high-value decisions.
- Continue AyurNod work on branch `website-optimization-and-updation` unless user changes direction.

## Later Session (AyurNod chat sprint)
- Ran website (`:3000`) + backend (`:3001`) live on VPS for real-time UI testing.
- Completed major `/chat` UX upgrades:
  - responsive/collapsible sidebar (mobile drawer + overlay/ESC close)
  - dark/light toggle in chat header
  - improved message contrast in dark mode
  - input alignment/autosize fixes
  - AI avatar sizing/balance adjustments
- Removed top-right "Full Chat" button per user request.
- Added interaction upgrades:
  - stop generation
  - retry/regenerate
  - edit last user message + resend
  - keyboard shortcut for sidebar toggle
  - improved markdown rendering (lists, links, code blocks + copy)
- Replaced dummy chat history with real backend-driven session history.
- Added backend chat APIs for sessions/list/history/delete and integrated in sidebar.
- Added reaction + voice layer:
  - like/dislike feedback per assistant message (persisted)
  - ElevenLabs TTS playback for assistant replies
  - microphone dictation (Web Speech API) in chat composer
- Identified and fixed production issue where `/api/chat` failed hard when provider auth failed.
  - Implemented safe fallback response path to avoid 500 crash loops.

## Infra / Runtime Notes
- OpenRouter old key produced 401 (`User not found`), causing AI replies to fail.
- Updated runtime key to user-provided replacement and validated live reply path.
- Tested multiple free-model candidates; many had upstream 429/empty-output constraints.
- Current practical free-model priority for backend runtime:
  1. `liquid/lfm-2.5-1.2b-instruct:free`
  2. `arcee-ai/trinity-large-preview:free`
  3. fallback models with higher 429 risk (`qwen3-4b`, `mistral-small-3.1-24b` free)

## Git (AyurNod)
Commits pushed on `website-optimization-and-updation` during this sprint:
- `46054bd` feat(website): optimize media + improve chat responsive UX
- `dd95772` feat(chat): add stop/retry flow, sidebar search, and local persistence
- `957914e` feat(chat): add streaming UX, regenerate, and edit-last-message flow
- `f72795b` feat(chat): improve markdown rendering with links, lists, and code blocks
- `2e7cc0b` feat(chat): replace dummy sidebar with real session history APIs
- `bf7de88` feat(chat): add mic input, feedback reactions, and ElevenLabs reply audio
- `cc3bfef` fix(chat): prevent 500s by returning safe fallback when LLM provider fails
